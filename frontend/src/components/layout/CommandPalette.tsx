import React from 'react'

/**
 * CommandPalette is now superseded by the AnchoredGlobalSearch dropdown
 * anchored directly beneath the search bar in the top navigation bar.
 */
export function CommandPalette() {
  return null
}

export default CommandPalette
